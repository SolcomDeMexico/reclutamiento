<%= form_for(@departamento, :html => {:id => "forma_departamento"}) do |f| %>
<% if @departamento.errors.any? %><br/><center>
    <div id="error_explanation">
      <h2><%= pluralize(@departamento.errors.count, "error no permite", "errores no permiten") %> registrar este departamento:</h2>
	<br/>
      <ul>     
          <% @departamento.errors.each do |attribute, errors_array| %>
 
            <li><%= errors_array %></li>
            <% end %>

      </ul>
    </div></center>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />	
    <%= f.text_field :nombre%>
  </div>
    <div class="field">
    <%= f.label :descripción %><br />	
    <%= f.text_area :descripcion%>
    <%= f.hidden_field :area_id, :value => params[:area] %>
  </div>
  <div class="field">
    <%= f.label :jefe %><br />
    <%= autocomplete_field_tag 'jefe','', autocomplete_usuario_display_name_departamentos_path, :update_elements => {:id => '#departamento_usuario_id' } %>
    <%= f.hidden_field :usuario_id %>
  </div>
      <div>
  <%= f.submit "Guardar" , "data-theme" => "b",  "data-inline" => "true"%>
  </div>

<% end %>
